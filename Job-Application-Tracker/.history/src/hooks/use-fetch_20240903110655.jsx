const { useState } = require("react");

const useFetch = (cb, Option = []) => {
    const [data, setData] = useState(undefined);
    const [loading, setLoading] = useSa

    const { session } =useSession();
}
